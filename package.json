{
    "scripts": {
        "dev": "pipenv run flask run",
        "migrate": "pipenv run flask db migrate && pipenv run flask db upgrade"
    },
    "dependencies": {
        "@fullhuman/postcss-purgecss": "^4.0.3",
        "autoprefixer": "^10.2.5",
        "postcss": "^8.2.10",
        "postcss-cli": "^8.3.1",
        "tailwindcss": "^2.1.1"
    }
}
